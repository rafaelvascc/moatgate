@model IList<IdentityServer4.EntityFramework.Entities.ClientRedirectUri>
<section data-scope="client-partials-redirect-uri">
    <table class="table table-condensed table-responsive table-striped table-hover table-bordered" id="tbl-redirect-uri">
        <thead>
            <tr>
                <th>
                    Redirect Uri
                </th>
                <th class="column-control-1">
                    <button type="button" class="btn btn-sm btn-primary pull-right" id="btn-add-redirect-uri" title="Add Redirect URL"><i class="fas fa-plus"></i></button>
                </th>
            </tr>
        </thead>
        <tbody>
            @{ int i = 0; }
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        <input type="hidden" class="form-control" name="Client.RedirectUris[@i.ToString()].Id" value="@item.Id" />
                        <input type="url" class="form-control" placeholder="/signin-oidc" name="Client.RedirectUris[@i.ToString()].RedirectUri" value="@item.RedirectUri" />
                    </td>
                    <td>
                        <div class="btn-group btn-group-sm pull-right">
                            <button type="button" class="btn btn-sm btn-danger  btn-delete-redirect-uri" title="Delete"><i class="fas fa-trash-alt"></i></button>
                        </div>
                    </td>
                </tr>
                i++;
            }
        </tbody>
    </table>
    <script id="redirect-uri-template" type="text/x-jsrender">
        <tr>
            <td>
                <input type="hidden" class="form-control" name="Client.RedirectUris[{{:index}}].Id" value="{{:id}}" />
                <input type="url" placeholder="/signin-oidc" class="form-control" name="Client.RedirectUris[{{:index}}].RedirectUri" value="https://somedomain.com/signin-oidc" />
            </td>
            <td>
                <div class="btn-group btn-group-sm pull-right">
                    <button type="button" class="btn btn-sm btn-danger btn-delete-redirect-uri" title="Delete"><i class="fas fa-trash-alt"></i></button>
                </div>
            </td>
        </tr>
    </script>
</section>